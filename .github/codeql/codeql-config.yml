name: "wirelog CodeQL Configuration"

queries:
  - uses: security-and-quality

paths:
  - wirelog
  - tests

paths-ignore:
  - bench
  - build*
  - scripts
  - examples
  - subprojects

threat-models:
  - remote
  - local
