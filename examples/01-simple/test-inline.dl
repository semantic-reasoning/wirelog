.decl parent(x: symbol, y: symbol)
.decl ancestor(x: symbol, y: symbol)

parent("alice", "bob").
parent("bob", "charlie").

.output ancestor("test_output.csv")

ancestor(X, Y) :- parent(X, Y).
ancestor(X, Z) :- parent(X, Y), ancestor(Y, Z).
