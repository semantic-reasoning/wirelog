# wirelog/util/meson.build - logger sources.

wirelog_util_log_src = files(
  'log.c',
  'log_emit.c',
)

# Test-only emit capture. Never linked into libwirelog.
wirelog_log_testhook_sources = files('log_testhook.c')
